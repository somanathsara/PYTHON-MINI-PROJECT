import os
data  = input("Enter command here: ")
command = data.split(maxsplit=2)
print(command)
# cmd = command[0]
# source = command[1]
# destination = command[2]
# print(f"{cmd}\n,{source},\n{destination}")